# Тестовое задание для Cuva

## Инструкция по сборке

```bash
# Установка зависимостей
$ npm install

# Запуск в dev режиме localhost:3000
$ npm run serve

# Сборка проекта, собранный проект находится в папке /dist
$ npm run build

```
